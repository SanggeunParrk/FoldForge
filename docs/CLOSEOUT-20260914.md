# Integration follow-up — 2026-09-14

## Operational inputs

The original MiniWorld configuration paths do not exist on this server, but
`/public_data02/BioMolDB_20260224` contains the operational stores used to produce
the earlier exports. Ordinary FoldForge readers now directly loaded its
`a3m.lmdb` and `template.lmdb` records for 1UBQ, 3PTB, 1A1K and 4YX2.
Six distinct protein entities were read, with 429–46,338 MSA rows and four aligned
templates per entity. The DB was opened read-only; no format conversion or
replacement DB was needed. This is sampled operational compatibility, not a full
record-by-record database audit. The synthetic RNA fixture remains separate;
operational RNA-MSA coverage is not established by these six protein entities.

`configs/inference/operational-1ubq.yaml` is an executable cluster example with
its typed FASTA alongside it. `docs/operational-input-results.json` records the
keys, paths and resource counts. CCD remains the qualified MiniWorld CCDMol DB.

## Cache build repair

The old script used removed capture APIs/environment switches and only constructed
ESMFold2. The replacement invokes the same released-model CLI for AF3, Protenix,
OpenDDE and ESMFold2. It preserves native precision/configuration, initializes
capture before the first launch, reuses completed round measurements, and writes
provenance-bearing shards into a private cache. Successful units and independently completed profiles merge under the engine lock.
See [CACHE-BUILD.md](CACHE-BUILD.md) for build and inference commands.

Timeout cleanup reuses the engine's process-tree termination, including compiler
children that create independent sessions. Seven builder regression tests pass:
exit-code/log preservation, ordinary/detached descendant termination, and
rejection of PyTorch baseline configurations as engine cache builds, and safe
recovery of complete profiles without promoting partial or incompatible searches.
The initial unoptimized startup run was intentionally interrupted after 390 s;
its partial shard and log remain under `validation/closeout-cache/af3-1ubq`.
The corrected `-v2` run and resumed `-v3` run have separate output directories.

## Repository state

The separate team-gm checkout is committed at 4e21ede on
`exp/miniworld-integrated`. Its A6000 full suite passed 365 tests with one skip;
its supplemental shared-runtime/checkpoint suite passed 16 tests. These counts
have overlap. The shared execution/layout sources match FoldForge's member at
8ca97b8; each branch retains its own existing model/training interfaces.

Remote team-gm exp/miniworld independently advanced to d5e934e. Publishing the
FoldForge member uses a separate integration branch so the existing remote commit
is preserved. Automatic approval review rejected the initial pushes pending
explicit approval for these repositories; no forced update is planned.

## Extended qualification

The A6000 large-complex and A5000 full-schedule runs have finished. Cache gap
builds remain in progress. Raw logs are under ignored `validation/closeout-*`;
job submission alone is not a passing result.

### A5000, full schedule

All five checkpoints completed on gpu02 / Slurm 1684652 with the operational
1UBQ input, native BF16, real denoiser compilation and explicit CUDA graphs.
The first two replay comparisons against the same compiled callable observed
zero maximum absolute difference. AF-family used 10 recycles and 200 diffusion
steps; ESMFold2 used its released defaults (10 actual denoiser calls per forward)
and cached ESMC. MSA depth 128, one sample, one warm repeat. This is execution
qualification, not a claim of biological accuracy or full A5000 cache coverage.

| Checkpoint | Warm full forward (s) | Peak allocated (GiB) | Compiled graphs |
| --- | ---: | ---: | ---: |
| af3 | 3.841 | 1.048 | 1 |
| esmfold2 | 0.563 | 0.800 | 13 |
| opendde | 4.279 | 1.845 | 1 |
| protenix | 4.298 | 1.020 | 1 |
| protenix-v2 | 4.178 | 1.019 | 1 |

The measurements exclude input featurization/output decoding; timings from
separate processes are not controlled speedup ratios. Raw records are in
`qualification-a5000-results.json`.

### A6000, large-complex comparison

All eight execution runs completed for 4YX2 (594 residues), with operational
MSA/template data, native BF16, MSA depth 128, seed 0 and one warm repeat.
Both modes use CUDA graphs; the second additionally compiles the denoiser.
Graph replays matched their respective callables exactly. Final structures are
compared separately using the unchanged CA RMSD <= 0.25 A / mean token pLDDT
difference <= 1 point criteria.

| Model | Graph (s) | Compile + graph (s) | CA RMSD (A) | Numerical comparison |
| --- | ---: | ---: | ---: | --- |
| AF3 | 55.565 | 52.007 | 0.323017 | **FAIL** |
| ESMFold2 | 5.075 | 4.729 | 0.244138 | PASS |
| OpenDDE | 216.794 | 212.756 | 0.156358 | PASS |
| Protenix | 50.647 | 46.691 | 0.209050 | PASS |

These are full forward times excluding input processing/output decoding, not
controlled speedup estimates. AF-family used 10 recycles / 200 diffusion steps;
ESMFold2 used its released schedule and cached ESMC. Numerical pass means this
specific comparison passed, not a biological-accuracy qualification. Protenix v2
was not included in this large-input comparison. Raw metrics and criteria are in
[qualification-large-results.json](qualification-large-results.json).

### Large-input numerical limitation

AF3 / 4YX2 (594 protein residues), operational MSA/template input: graph and
compile+graph both completed, and each replay matched its own callable exactly.
Their final structures differed by **0.323017 A CA RMSD**, exceeding the existing
0.25 A regression threshold; mean token pLDDT differed by 0.003374 points.
This numerical regression **failed**. The threshold is unchanged.

A diagnostic excluding the live shared NativeLayerNorm from compilation still
produced 0.321836 A RMSD and fragmented compilation into 40 graphs, hitting a
recompilation limit. It intercepted 4,954 BF16 and 229 FP32 normalization calls;
it does not support attributing the difference solely to normalization. An
initial probe patched the already-replaced upstream class and was ineffective.
Neither probe was promoted. Default `graph-bf16.yaml` keeps compilation disabled;
large-input compiler equivalence is not qualified by the passing 1UBQ result.

### Cache recovery and current build scope

The first full AF3 unit exhausted its initial 2,400 s budget. Its 1.9 MB shard
and nine round records were preserved. The resumed `-v3` unit uses a 7,200 s
budget and passed the earlier rounds through timing reuse; the interrupted
LayerNorm precompile then completed in 40 s using compiled artifacts.

Five fully searched keys (six workload profiles) were conservatively recovered
from the failed unit and merged after source/GPU/compiler provenance checks.
The model unit is still not marked complete. A fresh A6000 process replayed the
recovered LayerNorm shape `[1, 233472, 16]` with zero cache misses and passed
PyTorch parity at atol/rtol 0.02 (maximum absolute BF16 difference 0.03125).
This probe qualifies that key, not every remaining model shape.

The full real-model cache/replay matrix remains running in allocation 1684635;
ESMFold2's independent cache worker is job 1684985. Outputs are under
`validation/closeout-cache/`, with controller logs `closeout-cache-suite-v3.log`
and `closeout-cache-esm.log`. The source and shipped engine cache are preserved;
select the additional cache using `--engine-cache-dir`. Full coverage will only
be recorded after the fresh-process model replays pass without misses.

A durable continuation, Slurm **1686013**, is queued after allocation 1684635.
It skips successful fresh-process replays, recovers eligible saved profiles, and
adds explicit Protenix v2 coverage: five checkpoints across five inputs (25 units).
It retains per-unit failures and has an eight-hour allocation limit; its submission
is not evidence that all 25 units have finished. Logs/status are
`validation/closeout-cache-continuation.log` and
`validation/closeout-cache/continuation-status.json`.
