"""Boltz-2 (MIT).

**Not ported.** Weights fetch on first run; point ``BOLTZ_CACHE`` at the
FoldForge checkpoint directory rather than letting it write to a home directory.

Record here, as the port is made: the block-by-block mapping onto team-gm, and
what the upstream inference defaults actually are. Quoting a speed number
against upstream's shipped defaults rather than its tuned ones is the easiest
way to produce a flattering, meaningless comparison.
"""
