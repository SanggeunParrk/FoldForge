"""Chai-1 (Chai Discovery).

**Not ported.** Weights fetch on first run; point ``CHAI_DOWNLOADS_DIR`` at the
FoldForge checkpoint directory.

Record here, as the port is made: the block mapping onto team-gm and any place
the released checkpoint's layout forces a conversion step (a ``convert.py``
alongside the model, as ESMFold2 has).
"""
